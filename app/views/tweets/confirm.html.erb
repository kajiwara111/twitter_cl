<h3>以下の内容で、tweetを投稿します</h3>

<%= form_with(model: @tweet, local: true) do |form| %>
  <%= form.hidden_field :content %>
  <p>tweet内容:<%= @tweet.content %></p>
  <%= form.submit  "投稿する" %>
<% end %>
<%= link_to '戻る', :back %> 